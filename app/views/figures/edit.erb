<h1>Edit Figure</h1>
<form action="/figures/<%= @figure.id%>" method="POST">
<input type="hidden" name="_method" value="patch">

  <p>Edit Name:</p>
  <input id="figure_name" type="text" name="figure[name]" value="<%= @figure.name%>">

    <h4>Edit Title(s)</h4>
    <%Title.all.each do |title|%>
    <input type="checkbox" name="figure[title_ids][]" value="<%=title.id%>" id="<%=title.name%>" <%='checked' if @figure.titles.include?(title)%>><%=title.name%></input>
    <%end%>

    <br></br>
  <p>Edit Landmark(s)</p>
    <%Landmark.all.each do |landmark|%>
    <p><input type="checkbox" name="figure[landmark_ids][]" value="<%=landmark.id%>" id="<%=landmark.name%>" <%='checked' if @figure.landmarks.include?(landmark)%>><%=landmark.name%></input></p>
    <%end%>

 
    <br></br>
  <h4>Create a New Title:</h4>
    <input id="new_title" type="text" name="title[name]">

  <h4>Create a Landmark Name:</h4>
    <p>Name:</p>
    <input id="new_landmark" type="text" name="landmark[name]">
    <p>Year Completed:</p>
    <input type="text" name="landmark[year_completed]">

  <input type="submit" value="Edit Figure">
</form>